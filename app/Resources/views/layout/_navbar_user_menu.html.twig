<li class="page_user_block user user-menu">
    <!-- Menu Toggle Button -->
    {% if (not app.user)%}
        <a href="#/login" class="dropdown-toggle" data-toggle="dropdown" style="padding: 10px;">
            {#<a href="{{path('fos_user_security_login')}}" style="padding: 10px;">#}
            <img src="{{asset('assets/img/user.png')}}" width="30px" height="30px" class="img-circle" style="opacity: 0.5; ">
            <span style="margin-left: 5px;">{{ "link_login" | trans({}, 'SonataUserBundle')}}</span>
        </a>  
        <ul class="dropdown-menu">
            <li>
                <div class="col-lg-12" style="min-width: 330px; margin-top: 20px;">
                    {{render(controller('SonataUserBundle:SecurityFOSUser1:login',{'ajax-include':true},{'default': 'Loading...'} ))}}
                </div>
            </li>
        </ul>

    {% else %}
        <a href="#/user-actions" class="dropdown-toggle" data-toggle="dropdown" style="padding: 10px;">
            <span><i class="fa fa-envelope-o"></i> <label class="label label-info">4</label></span>
            <span><i class="fa fa-bell-o"></i> <label class="label label-warning">10</label></span>
            <span><i class="fa fa-flag-o"></i> <label class="label label-danger">9</label></span>

            {% if (app.user.media)%}
                {% media app.user.media, 'admin' with {'width':'30px','height':'30px','class':'img-circle', } %}
            {% else%}
                <img src="{{asset('assets/img/user.png')}}" width="30px" height="30px" class="img-circle">
            {% endif %}
        </a>
        <ul class="dropdown-menu">

            <li><a href="#/user">
                    {% if (app.user.media)%}
                        {% media app.user.media, 'admin' with {'width':'45px','height':'45px','class':'img-rounded', } %}
                    {% else%}
                        <img src="{{asset('assets/img/user.png')}}" width="45px" height="45px" class="img-rounded">
                    {% endif %}
                    <span class="">{{(app.user.firstname?app.user.fullname:app.user.username) | truncate(40)}}</span>
                </a>
            </li>
            {#
            <li class="divider"></li>
            <li><a href="#"><i class="fa fa-envelope-o"></i> Messages (4 unread)</a></li>
            <li class="divider"></li>
            <li><a href="#"><i class="fa fa-bell-o"></i> Notifications (10 news)</a></li>
            <li class="divider"></li>
            <li>
                <a href="#">
                    <i class="fa fa-users text-aqua"></i> 3 new members joined today
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="fa fa-tasks text-aqua"></i> 7 new tasks pending
                </a>
            </li>
            <li class="divider"></li>
            <li><a href="#"><i class="fa fa-flag-o"></i> Tasks (45 % completed)</a></li>
            <li class="divider"></li>
            <li>
                <a href="#">
                    <!-- Task title and progress text -->
                    <span>Design buttons</span>
                    <small class="pull-right" style="margin-left: 5px;">80%</small>
                    <!-- The progress bar -->
                    <div class="progress xs" style="margin-bottom: 0px;">
                        <!-- Change the css width attribute to simulate progress -->
                        <div class="progress-bar progress-bar-aqua" style="width: 80%" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100">
                            <span class="sr-only">80% Complete</span>
                        </div>
                    </div>
                </a>
            </li>
            <li>
                <a href="#">
                    <!-- Task title and progress text -->
                    <span>Pendings jobs</span>
                    <small class="pull-right" style="margin-left: 5px;">25%</small>
                    <!-- The progress bar -->
                    <div class="progress xs" style="margin-bottom: 0px;">
                        <!-- Change the css width attribute to simulate progress -->
                        <div class="progress-bar progress-bar-danger" style="width: 25%" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100">
                            <span class="sr-only">25% Complete</span>
                        </div>
                    </div>
                </a>
            </li>
            #}
            <!-- -->{% if (is_granted('ROLE_SONATA_ADMIN'))%}
            <li class="divider"></li>
            <li class="">
                <a href="{{path('sonata_admin_dashboard')}}"><i class="fa fa-cog"></i> {{ 'title_dashboard' | trans({}, 'SonataAdminBundle') | raw}}</a>
            </li>
            <!-- -->{% endif%}
            <li class="divider"></li>
            <li class="">
                <a href="{{path('fos_user_profile_show')}}"><i class="fa fa-user"></i> {{ "title_user_account" | trans({}, 'SonataUserBundle')}}</a>
            </li>
            <li class="divider"></li>
            <li class="">
                <a href="{{path('fos_user_security_logout')}}"><i class="fa fa-share"></i> {{ "link_logout" | trans({}, 'SonataUserBundle')}}</a>
            </li>
        </ul>
    {% endif%}
</li>