{{ admin.trans('log.new_action', {}, admin.translationDomain) }} <strong>{{ admin.trans(log.source.typLabel, {}, 'AppBundle') }}</strong> 
[<a href="javascript:void(0);" data-toggle="modal" data-target="#myModal_{{log.id}}">{{log.source.label | truncate(50)}}</a>] {{ admin.trans('log.new_action_label', {}, admin.translationDomain) }}.
<div class="modal fade" id="myModal_{{log.id}}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header modal-header-blue">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">{{ audit_assoc(log.source) }}</h4>
      </div>
      <div class="modal-body">
        {{ audit_diff(log, admin) }}
      </div>
    </div>
  </div>
</div>

